# DBを利用した匿名化フロー仕様書

## 1. 目的

本仕様書は、看護師がクローズド環境（電子カルテネットワーク）から入力した文章を即時匿名化し、DMZ経由でオープン環境に渡してChatGPT Businessによる文章整形を行い、再びクローズド環境へ戻すためのシステム構成と処理フローを示す。

対象例:
- 委員会議事録
- インシデント報告
- 入院時サマリー
- 退院時サマリー
- 中間サマリー
- 看護計画

## 2. 全体フロー

1. クローズ環境の Django 画面で文章を入力または貼り付け
2. 「匿名化する」ボタンにより Python 匿名化モジュールを実行
3. 匿名化結果を画面に表示し、必要に応じて PostgreSQL に保存
4. 匿名化済テキストを ChatGPT用プロンプト形式で出力し、DMZ へ配置
5. DMZ でウイルスチェックを実施
6. 共有フォルダを通じてオープン環境でプロンプトを利用
7. ChatGPT Business に入力して文章整形・要約・看護計画案を生成
8. 整形済テキストを DMZ 経由でクローズ環境に戻す
9. 返却ファイルの匿名化復元処理を実行し、元の実データへマッピング
10. クローズ環境で結果を確認し、必要であれば電子カルテへ転記またはファイル取込

## 3. 環境構成

### 3.1 クローズ環境（電子カルテネットワーク）

- Django アプリケーション
- Python 匿名化モジュール
- PostgreSQL（ログ保存・監査用途）
- 入力用画面、匿名化表示画面
- DMZ 連携用ファイル出力機能

### 3.2 DMZ

- 匿名化結果ファイルの受渡し
- ウイルスチェック
- 安全なファイル転送のみを扱う単純な構成

### 3.3 オープン環境（業務ネットワーク ）

- ChatGPT Business 利用者
- DMZ 経由でプロンプトファイルを受信
- 整形結果 JSON ファイルとして保存
- DMZ へ戻す

## 4. 匿名化処理

### 4.1 対象データ

- 氏名
- 住所
- 日付
- 時刻
- 固有名詞
- 医薬品名などの特定情報

### 4.2 匿名化方針

- 氏名: 仮名または「患者A」などのラベルへ置換
- 住所: 市区町村レベルに一般化
- 日付: 「X月上旬」などに一般化
- 時刻: 「午前/午後」レベルへ一般化
- 固有名詞: 種類名に置換（例：薬剤名 → "抗精神病薬"）

### 4.3 実装構成

既存の匿名化モジュール構成:

```
anonymizer/
├── config/
│   └── columns.yml
├── logs/
│   └── .gitkeep
├── modules/
│   ├── loader.py
│   ├── checker.py
│   ├── anonymize.py
│   ├── reporter.py
│   └── utils.py
├── cli.py
├── requirements.txt
└── README.md
```

- `loader.py`: 設定・外部データの読み込み
- `checker.py`: 匿名化対象判定
- `anonymize.py`: 実際の変換処理
- `reporter.py`: 処理ログ・レポート出力
- `utils.py`: 共通ユーティリティ

### 4.4 依存パッケージ

最低限の要件:
- pandas
- openpyxl
- faker
- pyyaml

## 5. Django 連携仕様

### 5.1 入力画面

- フリーテキスト入力フィールド
- 「匿名化する」ボタン
- 匿名化済み結果の表示領域
- 必要に応じて保存フラグを選択

### 5.2 送信処理

- 入力テキストを匿名化モジュールへ送信
- 匿名化実行後、画面に結果を表示
- 保存が必要な場合は PostgreSQL に書き込み

### 5.3 ファイル出力

- 匿名化結果を DMZ 連携用に JSON 形式で出力
- JSON によって `prompt` や `metadata` を構造化して渡す
- 出力ファイル名例: `prompt_YYYYMMDD_HHMM.json`
- DMZ 共有フォルダへ配置するための保存先を指定

## 6. DMZ 連携仕様

### 6.1 ファイル形式

- JSON ファイル（.json）を標準とする
- テキスト形式ではなく、構造化データとして `prompt` / `result` / `metadata` を送受信する
- 必要に応じて、人が参照しやすい形式で `content` を抽出・表示する

### 6.2 ファイル名

- 入力用プロンプト: `prompt_YYYYMMDD_HHMM.json`
- 返却用結果: `result_YYYYMMDD_HHMM.json`

### 6.3 JSON スキーマ例

- 入力用ファイル例:
  ```json
  {
    "id": "prompt_20260518_0830",
    "template_type": "入院時サマリー",
    "prompt": "あなたは精神科病棟の看護師です。以下の匿名化された情報をもとに、...",
    "prompt_text": "あなたは精神科病棟の看護師です。以下の匿名化された情報をもとに、入院時サマリーを作成してください...",
    "content": {
      "text1": "...",
      "text2": "...",
      "text3": "...",
      "text4": "..."
    },
    "metadata": {
      "source_id": "record_12345",
      "created_at": "2026-05-18T08:30:00Z"
    }
  }
  ```
- 返却用結果例:
  ```json
  {
    "id": "result_20260518_0835",
    "source_id": "prompt_20260518_0830",
    "result_text": "生成された整形済み文章...",
    "metadata": {
      "processed_at": "2026-05-18T08:35:00Z",
      "reviewer": "user_a"
    }
  }
  ```

### 6.4 セキュリティ

- DMZ ではファイル受け渡しのみを許可
- 可能であればウイルスチェックを実施
- 余計なネットワーク接続を排除

### 6.5 匿名化復元用メタデータ

- 匿名化処理時に置換した実データと匿名ラベルの対応表を生成
- 復元用メタデータはクローズ環境内の PostgreSQL または安全なファイルストレージに保存
- メタデータには以下を含む
  - 入力元テキスト ID
  - 匿名化ラベルの一覧（患者A、A病院など）
  - 実データ対応値
  - 元テキストの文脈情報や変換ルール
- DMZ へ送出するファイルには復元用実データを含めない
- 返却ファイルの復元は、クローズ環境内でのみ実行

## 7. Open 環境向けプロンプト構造

匿名化後テキストを ChatGPT Business に貼り付けるためのテンプレートを生成する。

### 7.0 ChatGPT への貼り付けと JSON 変換

- DMZ 受け渡しは JSON 形式を標準とするが、ChatGPT Business へ貼り付ける際は `content` を人が読みやすいテキストに変換して利用する。
- JSON をそのまま ChatGPT に貼り付けしても問題はないが、GPT 側の入力が明確になるように `prompt_text` を生成して提示する運用を推奨する。
- 例: `prompt` フィールドと `content` を組み合わせて、ChatGPT に貼り付ける文章を作成する。
- ChatGPT からの整形済み出力は多くの場合テキスト形式になるため、クローズ環境へ戻す前に `result_text` を含む JSON に再変換する仕組みを用意する。
- 変換機能は以下のいずれかで実装する。
  - ChatGPT 出力テキストを貼り付けて `result_*.json` を生成する変換ツール
  - ChatGPT に直接 JSON 出力を指示し、`result` フィールド付き JSON を生成させるプロンプト
- 変換後の JSON は `source_id` で元の `prompt` に紐付け、復元処理や監査に利用する。

### 7.1 入院時サマリー

```
あなたは精神科病棟の看護師です。
以下の匿名化された情報をもとに、入院時サマリーを作成してください。

【入院理由】

【入院時の状態】

【既往歴・生活歴】

【初期治療方針】

出力形式：
1. 入院時サマリー（400〜600字）
2. 看護上の初期アセスメント（箇条書き）

入力テキスト
{{ text1 }}

```

### 7.2 退院時サマリー

```
以下の匿名化された情報をもとに、退院時サマリーを作成してください。

【入院中の経過】

【治療内容】

【退院時の状態】

【今後の生活・支援】

出力形式：
1. 退院時サマリー（400〜600字）
2. 今後の看護・支援のポイント（箇条書き）

入力テキスト
{{ text1 }}

```

### 7.3 中間サマリー

```
以下の匿名化された情報をもとに、中間サマリーを作成してください。

【症状の変化】

【介入内容】

【現在の課題】

出力形式：
1. 中間サマリー（300〜500字）
2. 今後の看護方針（箇条書き）

入力テキスト
{{ text1 }}

```

### 7.4 インシデントレポート

```
以下の匿名化された情報をもとに、中間サマリーを作成してください。

1. インシデントレポート本文（300〜500字）
2. 再発防止策（箇条書き）
以下の自由記載テキストを基に、院内医療安全委員会提出用の
「様式1・様式2・様式3」を作成してください。

【生成ルール】
・必ず様式1 → 様式2 → 様式3 の順で作成する
・事実のみを記述し、推測や感情表現は含めない
・患者名・職員名はラベル（患者A、看護師A、医師A など）をそのまま使用する
・文章は時系列で整理する
・分類項目は必ず固定の選択肢から選ぶ
・Word に貼り付けて使えるように見出しと段落を整える
・事実記録は文章整形する。

【様式1：インシデント発生報告書（基本情報・分類）】
1. 発生日時
2. 発生場所
3. 当事者
4. 発見者
5. 関係職員
6. インシデント分類（以下から必ず選択）
   - 転倒・転落
   - 誤嚥
   - 誤薬
   - その他
7. インシデントレベル（レベル0〜5のいずれか）
8. 原因分類（患者要因・環境要因・職員要因・システム要因）
9. 事実の記録（時系列）

【様式2：要因分析・再発防止策】
10. 発生要因（事実ベース）
11. 再発防止策（一般的な医療安全の観点）

【様式3：改善計画書】
12. 改善目標
13. 改善策（具体的行動）
14. 評価方法

【入力テキスト】
{{ text1 }}
```

### 7.5 委員会議事録

```
以下の匿名化された情報をもとに、委員会議事録を作成してください。

【会議の目的】

【議論内容】

【決定事項】

【次回までの課題】

出力形式：
1. 議事録本文（400〜600字）
2. 決定事項（箇条書き）
【入力テキスト】
{{ text1 }}
```

### 7.6 看護計画

```
以下の匿名化された情報をもとに、看護計画を作成してください。

【アセスメント】

【問題点】

【目標】

【介入案】

出力形式：
1. 看護計画（NANDA/NOC/NICに準拠した形式）
2. 介入案（箇条書き）
【入力テキスト】
{{ text1 }}
```

## 8. 保存・ログ管理

- PostgreSQL は必要に応じてログ保存・監査目的で使用
- 保存しない場合は通過型処理として扱う
- 匿名化履歴、復元メタデータ、ファイル出力履歴、プロンプト生成履歴を記録可能

## 9. 制約と注意点

- CSV 一括処理ではなく、1件ずつ即時処理を前提とする
- 匿名化済み情報はオープン環境に出すため、特定可能な情報は厳格に除去・一般化する
- DMZ はファイル連携のみを行い、双方向通信は避ける
- ChatGPT Business への入力内容は必ず匿名化後のテキストとする
- 返却ファイルの復元はクローズ環境内でのみ実行し、復元メタデータは DMZ に送らない
- 自動復元はラベルベースの置換に限定し、生成内容の意味変更には手動確認を必須とする

## 10. 今後の拡張案

- Django 画面上で複数テンプレートを選択できる機能
- 生成プロンプトの自動保存・再利用
- DMZ 側のファイル受信確認・差分検出
- PostgreSQL への監査ログの細分化
- 匿名化ルールの設定画面化
