# 匿名化フロー仕様書

## 1. 目的

本仕様書は、CloseSide で入力した文章を匿名化し、DMZ 経由で OpenSide に渡して ChatGPT で整形し、その結果を CloseSide で復元確認する現在の実装フローを整理したものである。

現行実装は PostgreSQL 接続設定を備え、ローカルディレクトリを使う検証構成である。環境変数未設定時は SQLite にフォールバックする。

## 2. 前提

### 2.1 画面と役割

- CloseSide: 匿名化入力、ラベル編集、患者/職員/患者関連者 master 管理、プロンプト作成、DMZ 出力、返却 JSON 取り込み、履歴確認。患者関連者 master は `linked_person_code` を個別コードとして自動採番し、`枝番` は患者内の整理番号として保持する。
- OpenSide: DMZ 上のプロンプト一覧表示、取り込み、ChatGPT 用表示、生成結果の返却 DMZ 出力
- 共通: `anonymizer/` の匿名化・復元・JSON 生成ロジック

### 2.2 主要ファイル

- CloseSide 画面: `webapp/close_side/views.py`
- OpenSide 画面: `webapp/open_side/views.py`
- 匿名化ロジック: `anonymizer/modules/anonymize.py`
- テンプレート正本: `webapp/anonymizer_app/prompt_templates/*.txt`
- ローカル DMZ: `dmz/close_to_open/` と `dmz/open_to_close/`

### 2.3 ネットワーク制御

アプリケーション層では `NETWORK_POLICY` により `/close/` と `/open/` のアクセス元 CIDR を確認する。設定値は次のとおりである。

- CloseSide: `192.168.50.0/24`
- OpenSide: `192.168.110.0/24`
- DMZ: `192.168.150.0/24`

DEBUG 時は loopback を許可する。

## 3. 全体フロー

```text
CloseSide
  1. 文章入力
  2. anonymize_text() で匿名化
  3. restore_map / prompt_json を保存
  4. DMZ へ prompt_*.json を出力
        ↓
DMZ (close_to_open)
        ↓ 取り込み
OpenSide
  5. プロンプトを表示して ChatGPT に貼り付け
  6. 生成結果をフォームに貼り付け
  7. result_*.json を DMZ へ出力
        ↓
DMZ (open_to_close)
        ↓ 取り込み
CloseSide
  8. 返却 JSON を取り込み
  9. restore_map で復元
  10. 結果確認・履歴保存
```

## 4. CloseSide のフロー

### 4.1 匿名化入力

`/close/anonymize/` で `AnonymizeForm` によりテンプレートと本文を受け取る。

free 入力では、手入力に加えてファイルの読み込みにも対応する。対応するのは `.txt`、`.md`、`.csv`、`.log`、`.json` に加え、Excel（`.xlsx`）、Word（`.docx`）、PDF（`.pdf`）である。PDF は文字情報を含む文書を対象とし、画像のみのスキャン PDF は対象外とする。

処理順は以下である。

1. `anonymize_text()` を実行する
2. 匿名化後本文と `restore_map` を得る
3. `source_id` を生成する
4. `build_prompt_payload()` でプロンプト JSON を作る
5. `RestoreMetadata` を保存する
6. `Prompt` を保存する
7. 画面に匿名化前後の差分、ラベル一覧、JSON を表示する

この画面ではラベルの追加・削除・文言修正ができ、`update_prompt_payload` API で `prompt_json` と `restore_map` を更新できる。

`anonymize_text()` では、患者 ID が指定されている場合は患者 master の氏名を優先ラベルへ置換し、併せて有効な職員 master の氏名も `看護師A`、`医師A`、`職員A` などの職種ラベル付きで置換する。さらに、同じ患者 ID に紐づく患者関連者 master があれば、`種別` と `属性` に応じたラベルで優先置換し、監査や検索では `linked_person_code` を主ID、`枝番` を補助キーとして扱う。

### 4.2 プロンプト JSON の保存

CloseSide で作成した `RestoreMetadata.prompt_json` は、DMZ 出力前の内部データである。`restore_map` は同じく CloseSide 内部に保持し、OpenSide へは渡さない。

### 4.3 DMZ 出力

`/close/dmz/export/` で `dmz/close_to_open/` に JSON を書き出す。

- ファイル名: `prompt_{source_id}.json`
- 出力内容: `RestoreMetadata.prompt_json`
- 追加される主な metadata: `source_id`, `owner_user_id`, `owner_username`, `sent_by`, `sent_at`

### 4.4 手動プロンプト

`/close/prompts/` 系の画面では、手動で `Prompt` を作成・編集・削除できる。`prompt_send_to_dmz` を使うと、匿名化フローを経ていない手動プロンプトも DMZ へ送れる。

`source_id` が無い手動プロンプトは `prompt_manual_{pk}_{8hex}` の形式で ID が発番される。

## 5. OpenSide のフロー

### 5.1 DMZ 一覧と取り込み

`/open/dmz/list/` で `dmz/close_to_open/` のファイル一覧を表示する。

`/open/dmz/import/` または一覧画面の「取込」ボタンでファイルを取り込むと、次の処理が行われる。

1. DMZ 上の JSON を読み込む
2. `webapp/anonymizer_app/logs/` にコピーする
3. DMZ 上の元ファイルを削除する
4. `RestoreMetadata` と `Prompt` の status を `imported_to_open` に更新する

### 5.2 ChatGPT 用表示

`/open/imported/<filename>/` では、取り込んだ JSON から `prompt_text` を表示する。

- JSON に `prompt_text` があればそれを使用
- 無ければ `prompt`、`content.text`、最終的に原文の順で表示
- 画面にはコピーボタンがあり、ChatGPT の Web 画面へ貼り付ける前提である

### 5.3 生成結果の返却

`/open/imported/<filename>/result/` に ChatGPT の生成結果を貼り付けると、`build_result_payload()` を使って返却 JSON を作成する。

- `result_text` は必須
- `reviewer` は任意
- `template_type` は元プロンプトから引き継ぐ

返却 JSON は `dmz/open_to_close/` に次の形式で保存される。

- ファイル名: `result_{source_id}_{YYYYMMDD_HHMMSS}.json`
- 内容: `id`, `source_id`, `result_text`, `metadata.processed_at`, `metadata.reviewer` に加え、OpenSide 側で `template_type`, `source_prompt_file`, `owner_user_id`, `owner_username`, `returned_by`, `returned_at` を付与する

## 6. CloseSide の返却取込と復元

### 6.1 返却 DMZ の一覧

`/close/dmz/results/` で `dmz/open_to_close/` のファイル一覧を表示する。

### 6.2 返却 JSON の取り込み

`/close/dmz/results/import/` で返却 JSON を取り込むと、次の処理を行う。

1. 返却 JSON を読み込む
2. `source_id` と `result_text` を確認する
3. `RestoreMetadata` を検索する
4. `restore_map` を使って `restore_text()` を実行する
5. `RestoredResult` を保存する
6. `RestoreMetadata.status` を `imported_to_close` に更新する
7. 取り込み済み DMZ ファイルを削除する
8. 差分ハイライト付きで結果を表示する

### 6.3 履歴確認

`/close/dmz/results/history/` では、取り込み済み結果の履歴を検索できる。`/close/dmz/results/history/<pk>/` では、選択した履歴のプレビューを表示する。

## 7. JSON 仕様

### 7.1 プロンプト JSON

CloseSide で生成するプロンプト JSON の基本構造は次のとおりである。

```json
{
  "id": "prompt_xxx",
  "template_type": "入院時サマリー",
  "title": "入院時サマリー",
  "prompt": "...",
  "prompt_text": "...",
  "content": {
    "text": "..."
  },
  "metadata": {
    "source_id": "prompt_xxx",
    "created_at": null
  }
}
```

### 7.2 復元 JSON

`download_restore` で出力される内部 JSON は、次の 2 項目のみを持つ。

```json
{
  "source_id": "prompt_xxx",
  "restore_map": {
    "匿名化後文言": "元文言"
  }
}
```

### 7.3 返却 JSON

OpenSide が作成し、CloseSide が取り込む返却 JSON は次の構造を基本とする。

```json
{
  "id": "result_prompt_xxx",
  "source_id": "prompt_xxx",
  "result_text": "...",
  "metadata": {
    "processed_at": null,
    "reviewer": "unknown"
  }
}
```

OpenSide から返却するときに、`template_type` などの補助情報が追加される。

## 8. 状態遷移

### 8.1 RestoreMetadata

| 状態 | 意味 |
|---|---|
| `draft` | 匿名化作成直後 |
| `sent_to_dmz` | CloseSide から DMZ 出力済み |
| `imported_to_open` | OpenSide 取り込み済み |
| `returned_to_dmz` | OpenSide から返却 DMZ 出力済み |
| `imported_to_close` | CloseSide 取り込み済み |

### 8.2 Prompt

| 状態 | 意味 |
|---|---|
| `draft` | 作成済み |
| `sent_to_dmz` | DMZ 出力済み |
| `imported_to_open` | OpenSide 取り込み済み |

### 8.3 RestoredResult

| 状態 | 意味 |
|---|---|
| `imported` | 取り込み済み |
| `deleted` | 削除済み |

## 9. 画面上の補助機能

- 匿名化前後の差分ハイライト
- 匿名ラベル一覧の編集
- プロンプト JSON / 復元 JSON の即時確認
- ChatGPT 貼り付け用のコピー操作
- テキスト、Excel、Word、PDF の読み込みによる本文投入
- 取り込み済み結果の履歴検索
- 操作ログの確認

## 10. 未完了課題

現時点で残っている課題は以下である。

- 実 DMZ、SFTP、ウイルスチェックの本番連携がない
- PostgreSQL への切り替え設定はあるが、本番 DB 運用の検証は別途必要
- ChatGPT との自動 API 連携はなく、OpenSide でのコピー&ペースト運用に依存している
- `anonymize` CLI は実匿名化ではなく、プロンプト JSON 生成のみになっている
- 匿名化ロジックは正規表現中心で、対象漏れや誤置換の検証が必要
- テンプレート txt の版管理・承認フローは未整備
- 自動テストがこのワークツリーでは確認できないため、回帰確認は手動依存である

## 11. 補足
df
このフローは、CloseSide で匿名化と復元、OpenSide で ChatGPT 用の文面準備と結果返却を分担することを前提にしている。

現状はプロトタイプのため、仕様書上の理想フローよりも、実装済みの画面遷移・JSON・ローカルファイル連携を基準に運用するのが安全である。
