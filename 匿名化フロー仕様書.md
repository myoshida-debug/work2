# DBを利用した匿名化フロー仕様書

## 1. 目的

本仕様書は、看護師がクローズド環境（電子カルテネットワーク）から入力した文章を即時匿名化し、DMZ経由でオープン環境に渡してChatGPT Businessによる文章整形を行い、再びクローズド環境へ戻すためのシステム構成と処理フローを示す。

対象例:
- 委員会議事録
- インシデント報告
- 入院時サマリー
- 退院時サマリー
- 中間サマリー
- 看護計画

## 2. 全体フロー

1. クローズ環境の Django 画面で文章を入力または貼り付け
2. 「匿名化する」ボタンにより Python 匿名化モジュールを実行
3. 匿名化結果を画面に表示し、必要に応じて PostgreSQL に保存
4. 匿名化済テキストを ChatGPT用プロンプト形式で出力し、DMZ へ配置
5. DMZ でウイルスチェックを実施
6. 共有フォルダを通じてオープン環境でプロンプトを利用
7. ChatGPT Business に入力して文章整形・要約・看護計画案を生成
8. 整形済テキストを DMZ 経由でクローズ環境に戻す
9. 返却ファイルの匿名化復元処理を実行し、元の実データへマッピング
10. クローズ環境で結果を確認し、必要であれば電子カルテへ転記またはファイル取込

## 3. 環境構成

### 3.1 クローズ環境（電子カルテネットワーク）

- Django アプリケーション
- Python 匿名化モジュール
- PostgreSQL（ログ保存・監査用途）
- 入力用画面、匿名化表示画面
- DMZ 連携用ファイル出力機能

### 3.2 DMZ

- 匿名化結果ファイルの受渡し
- ウイルスチェック
- 安全なファイル転送のみを扱う単純な構成

### 3.3 オープン環境（業務ネットワーク ）

- ChatGPT Business 利用者
- DMZ 経由でプロンプトファイルを受信
- 整形結果 JSON ファイルとして保存
- DMZ へ戻す

### 3.4 ネットワーク設計・セキュリティ方針

#### 基本方針

- CloseSide、DMZ、OpenSide は別セグメントに分離する
- CloseSide と OpenSide は直接通信しない
- データ連携は必ず DMZ を経由する
- DMZ は能動的に送信せず、データの一時保管領域として扱う
- CloseSide / OpenSide が DMZ へアクセスし、必要なデータを取り込む
- CloseSide / OpenSide 側から DMZ をポーリングまたは API 呼び出しで取り込む
- ファイアウォールで通信元、通信先、ポートを制限する

#### セグメント設計

| 区分 | ネットワーク帯 | 用途 |
|---|---:|---|
| CloseSide | `192.168.50.0/24` | 機密情報を扱う内部領域 |
| DMZ | `192.168.150.0/24` | CloseSide / OpenSide 間の中継領域 |
| OpenSide | `192.168.110.0/24` | 生成AI処理を行う外部連携領域 |

#### 通信制御

| 通信元 | 通信先 | 用途 | 方針 |
|---|---|---|---|
| CloseSide | DMZ | プロンプト送信、生成文章取込 | 許可 |
| OpenSide | DMZ | プロンプト取込、生成文章送信 | 許可 |
| DMZ | CloseSide | 能動送信 | 原則禁止。CloseSide からの取込方式を推奨 |
| DMZ | OpenSide | 能動送信 | 原則禁止。OpenSide からの取込方式を推奨 |
| CloseSide | OpenSide | 直接通信 | 禁止 |
| OpenSide | CloseSide | 直接通信 | 禁止 |

#### DMZ の扱い

DMZ はデータを能動的に配信するサーバーではなく、以下のような一時保管領域として設計する。

```text
CloseSide
  ↓ プロンプト送信
DMZ
  ↑ OpenSideが取り込み

OpenSide
  ↓ AI生成文章送信
DMZ
  ↑ CloseSideが取り込み
```

#### ログ記録項目

操作ログには以下を記録する。

| 項目 | 内容 |
|---|---|
| 操作者ID | 作成、送信、取込、削除したユーザー |
| 操作種別 | 送信、取込、削除、復元など |
| 対象ID | プロンプトID、生成文章ID |
| 送信元IP | 操作元IPアドレス |
| 取込元IP | 取込操作元IPアドレス |
| 実行日時 | 操作日時 |
| 結果 | 成功、失敗 |
| エラー内容 | 失敗時の詳細 |

Web アプリでは、アプリケーション側の補助制御として `NETWORK_POLICY` 設定を用意し、CloseSide / OpenSide の URL に対してアクセス元セグメントを検査する。実運用では、このアプリケーション制御に加えて、ネットワーク機器または OS 側のファイアウォールで通信元、通信先、ポートを制限する。

## 4. 匿名化処理

### 4.1 対象データ

- 氏名
- 住所
- 日付
- 時刻
- 固有名詞
- 医薬品名などの特定情報

### 4.2 匿名化方針

- 氏名: 仮名または「患者A」などのラベルへ置換
- 住所: 市区町村レベルに一般化
- 日付: 「X月上旬」などに一般化
- 時刻: 「午前/午後」レベルへ一般化
- 固有名詞: 種類名に置換（例：薬剤名 → "抗精神病薬"）

### 4.3 実装構成

既存の匿名化モジュール構成:

```
anonymizer/
├── config/
│   └── columns.yml
├── logs/
│   └── .gitkeep
├── modules/
│   ├── loader.py
│   ├── checker.py
│   ├── anonymize.py
│   ├── reporter.py
│   └── utils.py
├── cli.py
├── requirements.txt
└── README.md
```

- `loader.py`: 設定・外部データの読み込み
- `checker.py`: 匿名化対象判定
- `anonymize.py`: 実際の変換処理
- `reporter.py`: 処理ログ・レポート出力
- `utils.py`: 共通ユーティリティ

### 4.4 依存パッケージ

最低限の要件:
- pandas
- openpyxl
- faker
- pyyaml

## 5. Django 連携仕様

### 5.1 入力画面

- フリーテキスト入力フィールド
- 「匿名化する」ボタン
- 匿名化済み結果の表示領域
- 必要に応じて保存フラグを選択

### 5.2 送信処理

- 入力テキストを匿名化モジュールへ送信
- 匿名化実行後、画面に結果を表示
- 保存が必要な場合は PostgreSQL に書き込み

### 5.3 ファイル出力

- 匿名化結果を DMZ 連携用に JSON 形式で出力
- JSON によって `prompt` や `metadata` を構造化して渡す
- 出力ファイル名例: `prompt_YYYYMMDD_HHMM.json`
- DMZ 共有フォルダへ配置するための保存先を指定

## 6. DMZ 連携仕様

### 6.1 ファイル形式

- JSON ファイル（.json）を標準とする
- テキスト形式ではなく、構造化データとして `prompt` / `result` / `metadata` を送受信する
- 必要に応じて、人が参照しやすい形式で `content` を抽出・表示する

### 6.2 ファイル名

- 入力用プロンプト: `prompt_YYYYMMDD_HHMM.json`
- 返却用結果: `result_YYYYMMDD_HHMM.json`

### 6.3 JSON スキーマ例

- 入力用ファイル例:
  ```json
  {
    "id": "prompt_20260518_0830",
    "template_type": "入院時サマリー",
    "prompt": "あなたは精神科病棟の看護師です。以下の匿名化された情報をもとに、...",
    "prompt_text": "あなたは精神科病棟の看護師です。以下の匿名化された情報をもとに、入院時サマリーを作成してください...",
    "content": {
      "text": "..."
    },
    "metadata": {
      "source_id": "record_12345",
      "created_at": "2026-05-18T08:30:00Z"
    }
  }
  ```
- 返却用結果例:
  ```json
  {
    "id": "result_20260518_0835",
    "source_id": "prompt_20260518_0830",
    "result_text": "生成された整形済み文章...",
    "metadata": {
      "processed_at": "2026-05-18T08:35:00Z",
      "reviewer": "user_a"
    }
  }
  ```

### 6.4 セキュリティ

- DMZ ではファイル受け渡しのみを許可
- 可能であればウイルスチェックを実施
- 余計なネットワーク接続を排除

### 6.5 匿名化復元用メタデータ

- 匿名化処理時に置換した実データと匿名ラベルの対応表を生成
- 復元用メタデータはクローズ環境内の PostgreSQL または安全なファイルストレージに保存
- メタデータには以下を含む
  - 入力元テキスト ID
  - 匿名化ラベルの一覧（患者A、A病院など）
  - 実データ対応値
  - 元テキストの文脈情報や変換ルール
- DMZ へ送出するファイルには復元用実データを含めない
- 返却ファイルの復元は、クローズ環境内でのみ実行

## 7. Open 環境向けプロンプト構造

匿名化後テキストを ChatGPT Business に貼り付けるためのプロンプトテンプレートは、`temp/` 内のテキストファイルを正本とする。
Django アプリでは、これらを `webapp/anonymizer_app/prompt_templates/` に配置し、`load_prompt_templates` 管理コマンドで DB の `Template` に登録する。

テンプレートは、共通ルールを含む `default.txt` をベースとし、文書種別ごとの追加指示を結合して完成させる。

### 7.0 ChatGPT への貼り付けと JSON 変換

- DMZ 受け渡しは JSON 形式を標準とする。
- ChatGPT Business へ貼り付ける際は、JSON 内の `prompt` または `prompt_text` を利用する。
- 入力本文は単一項目とし、`content.text` に匿名化済み本文を格納する。
- テンプレート内の入力変数は `{request_no}`, `{document_type}`, `{anonymized_text}` を使用する。
- ChatGPT からの整形済み出力は、クローズ環境へ戻す前に `result_text` を含む JSON に変換する。
- 変換後の JSON は `source_id` で元の `prompt` に紐付け、復元処理や監査に利用する。

### 7.0.1 テンプレート一覧

| 文書種別 | 正本ファイル | 用途 |
|---|---|---|
| 共通テンプレート | `temp/default.txt` | 汎用院内文書 |
| 入院時サマリー | `temp/admission.txt` | 入院時サマリー（詳細版） |
| 退院時サマリー | `temp/discharge.txt` | 退院時サマリー |
| 中間サマリー | `temp/midterm.txt` | 中間サマリー |
| インシデントレポート（様式1-3） | `temp/incident.txt` | インシデント報告書（様式1・様式2・様式3） |
| インシデントレポート（簡易版） | `temp/incident2.txt` | インシデント報告書（簡易版） |
| 委員会議事録 | `temp/committee.txt` | 委員会議事録 |
| 看護計画 | `temp/nursing.txt` | 看護計画 |

### 7.1 共通テンプレート

- 正本ファイル: `temp/default.txt`
- 用途: 汎用院内文書

```text
あなたは医療文書作成支援AIです。
以下の匿名化済み文章をもとに、院内文書として使える自然で簡潔な文章に整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない情報を補わない
- 推測で診断名・原因・経過を追加しない
- 匿名化ラベル（患者A、家族（父）、看護師A、医師A、[電話番号]、[施設名]など）を変更しない
- 個人情報を復元しない
- 文体は医療記録として簡潔・客観的にする
- 口語表現は避ける
- 重複表現は整理する
- 不明な情報は「記載なし」または省略とする
- 箇条書き指定がない限り、読みやすい見出し付き文章で出力する

【表記ルール】
- 日付は入力どおりの匿名化済み表記を維持する
- 数値や検査値は入力にあるもののみ用いる
- 主語が曖昧な場合は、意味が変わらない範囲で明確化する
- 略語の多用を避け、必要に応じて自然な日本語に整える

【禁止事項】
- 匿名化ラベルの削除・変更
- 事実にない原因分析
- 医療判断の追加
- 患者や家族の感情の創作
- 断定的な補足

あなたは医療文書作成支援AIです。
以下の匿名化済み文章を、院内文書として自然で読みやすい文章に整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない情報を補わない
- 匿名化ラベルを変更しない
- 個人情報を復元しない
- 簡潔で客観的な文体にする

【出力条件】
- 時系列や論点を整理する
- 重複表現はまとめる
- 不明な情報は補わない
- 読みやすい見出し付きで整える

【受付番号】
{request_no}

【文書種別】
{document_type}

【入力本文】
{anonymized_text}
```

### 7.2 入院時サマリー

- 正本ファイル: `temp/admission.txt`
- 用途: 入院時サマリー（詳細版）

```text
あなたは医療文書作成支援AIです。
以下の匿名化済み文章をもとに、院内文書として使える自然で簡潔な文章に整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない情報を補わない
- 推測で診断名・原因・経過を追加しない
- 匿名化ラベル（患者A、家族（父）、看護師A、医師A、[電話番号]、[施設名]など）を変更しない
- 個人情報を復元しない
- 文体は医療記録として簡潔・客観的にする
- 口語表現は避ける
- 重複表現は整理する
- 不明な情報は「記載なし」または省略とする
- 箇条書き指定がない限り、読みやすい見出し付き文章で出力する

【表記ルール】
- 日付は入力どおりの匿名化済み表記を維持する
- 数値や検査値は入力にあるもののみ用いる
- 主語が曖昧な場合は、意味が変わらない範囲で明確化する
- 略語の多用を避け、必要に応じて自然な日本語に整える

【禁止事項】
- 匿名化ラベルの削除・変更
- 事実にない原因分析
- 医療判断の追加
- 患者や家族の感情の創作
- 断定的な補足

あなたは医療文書作成支援AIです。
以下の匿名化済み文章を、詳細版の入院時サマリーとして整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない情報を補わない
- 匿名化ラベルを変更しない
- A4 2枚相当（1500〜2000文字程度）に収める
- 医療者が引き継ぎに使える密度で整理する

【目的】
入院の背景、現在の状態、今後の治療方針を詳細に把握できるようにする。

【出力構成】
1. 主訴
2. 現病歴
3. 既往歴
4. 家族歴・生活歴
5. 内服薬・アレルギー
6. 入院時身体所見
7. 検査所見
8. 臨床評価
9. 入院目的
10. 治療方針
11. 留意点

【出力条件】
- 時系列が分かるように整理
- 医療上重要な情報を優先
- 冗長な表現を避ける
- 不明な項目は補完しない

【受付番号】
{request_no}

【文書種別】
{document_type}

【入力本文】
{anonymized_text}
```

### 7.3 退院時サマリー

- 正本ファイル: `temp/discharge.txt`
- 用途: 退院時サマリー

```text
あなたは医療文書作成支援AIです。
以下の匿名化済み文章をもとに、院内文書として使える自然で簡潔な文章に整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない情報を補わない
- 推測で診断名・原因・経過を追加しない
- 匿名化ラベル（患者A、家族（父）、看護師A、医師A、[電話番号]、[施設名]など）を変更しない
- 個人情報を復元しない
- 文体は医療記録として簡潔・客観的にする
- 口語表現は避ける
- 重複表現は整理する
- 不明な情報は「記載なし」または省略とする
- 箇条書き指定がない限り、読みやすい見出し付き文章で出力する

【表記ルール】
- 日付は入力どおりの匿名化済み表記を維持する
- 数値や検査値は入力にあるもののみ用いる
- 主語が曖昧な場合は、意味が変わらない範囲で明確化する
- 略語の多用を避け、必要に応じて自然な日本語に整える

【禁止事項】
- 匿名化ラベルの削除・変更
- 事実にない原因分析
- 医療判断の追加
- 患者や家族の感情の創作
- 断定的な補足

あなたは医療文書作成支援AIです。
以下の匿名化済み文章を、退院時サマリーとして整形してください。

【最重要ルール】
- 医学的意味を変えない
- 推測を加えない
- 匿名化ラベルを変更しない
- A4 1枚相当（800〜1000文字程度）に収める

【目的】
退院時点で、入院理由・経過・退院後方針を簡潔に把握できるようにする。

【出力構成】
1. 入院理由
2. 入院後経過
3. 実施治療
4. 主要検査結果
5. 退院時状態
6. 退院処方・継続治療
7. 今後の方針

【出力条件】
- 時系列で簡潔に整理
- 治療内容と結果を明確にする
- 退院後の注意点があれば含める
- 不明な内容は補わない

【受付番号】
{request_no}

【文書種別】
{document_type}

【入力本文】
{anonymized_text}
```

### 7.4 中間サマリー

- 正本ファイル: `temp/midterm.txt`
- 用途: 中間サマリー

```text
あなたは医療文書作成支援AIです。
以下の匿名化済み文章をもとに、院内文書として使える自然で簡潔な文章に整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない情報を補わない
- 推測で診断名・原因・経過を追加しない
- 匿名化ラベル（患者A、家族（父）、看護師A、医師A、[電話番号]、[施設名]など）を変更しない
- 個人情報を復元しない
- 文体は医療記録として簡潔・客観的にする
- 口語表現は避ける
- 重複表現は整理する
- 不明な情報は「記載なし」または省略とする
- 箇条書き指定がない限り、読みやすい見出し付き文章で出力する

【表記ルール】
- 日付は入力どおりの匿名化済み表記を維持する
- 数値や検査値は入力にあるもののみ用いる
- 主語が曖昧な場合は、意味が変わらない範囲で明確化する
- 略語の多用を避け、必要に応じて自然な日本語に整える

【禁止事項】
- 匿名化ラベルの削除・変更
- 事実にない原因分析
- 医療判断の追加
- 患者や家族の感情の創作
- 断定的な補足

あなたは医療文書作成支援AIです。
以下の匿名化済み文章を、中間サマリーとして整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない情報を補わない
- 匿名化ラベルを変更しない
- A4 1枚相当（800〜1000文字程度）に収める

【目的】
現時点までの経過と、現在の問題点・方針を簡潔に共有できるようにする。

【出力構成】
1. 入院目的・背景
2. 現在までの経過
3. 現在の状態
4. 問題点
5. 治療・対応
6. 今後の方針

【出力条件】
- 現在重要な点を優先
- 不要な重複を削る
- 短くても判断材料が分かるようにする

【受付番号】
{request_no}

【文書種別】
{document_type}

【入力本文】
{anonymized_text}
```

### 7.5 インシデントレポート（様式1-3）

- 正本ファイル: `temp/incident.txt`
- 用途: インシデント報告書（様式1・様式2・様式3）

```text
あなたは医療文書作成支援AIです。
以下の匿名化済み文章をもとに、院内文書として使える自然で簡潔な文章に整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない情報を補わない
- 推測で診断名・原因・経過を追加しない
- 匿名化ラベル（患者A、家族（父）、看護師A、医師A、[電話番号]、[施設名]など）を変更しない
- 個人情報を復元しない
- 文体は医療記録として簡潔・客観的にする
- 口語表現は避ける
- 重複表現は整理する
- 不明な情報は「記載なし」または省略とする
- 箇条書き指定がない限り、読みやすい見出し付き文章で出力する

【表記ルール】
- 日付は入力どおりの匿名化済み表記を維持する
- 数値や検査値は入力にあるもののみ用いる
- 主語が曖昧な場合は、意味が変わらない範囲で明確化する
- 略語の多用を避け、必要に応じて自然な日本語に整える

【禁止事項】
- 匿名化ラベルの削除・変更
- 事実にない原因分析
- 医療判断の追加
- 患者や家族の感情の創作
- 断定的な補足

あなたは医療文書作成支援AIです。
以下の匿名化済み文章を、インシデント報告書として整形してください。

【最重要ルール】
- 事実のみを記載する
- 推測は書かない
- 匿名化ラベルを変更しない

【目的】
発生状況、インシデントレベル、患者への影響、対応、再発防止策を簡潔に共有する。

以下の匿名化された情報をもとに、院内医療安全委員会提
出用の
「様式1・様式2・様式3」を作成してください。

【生成ルール】
・必ず様式1 → 様式2 → 様式3 の順で作成する
・事実のみを記述し、推測や感情表現は含めない
・患者名・職員名はラベル（患者A、看護師A、医師A など
）をそのまま使用する
・文章は時系列で整理する
・分類項目は必ず固定の選択肢から選ぶ
・Word に貼り付けて使えるように見出しと段落を整える
・事実記録は文章整形する

【様式1：インシデント発生報告書】
1. 発生日時
2. 発生場所
3. 当事者
4. 発見者
5. 関係職員
6. インシデント分類（転倒・転落／誤嚥／誤薬／興奮・暴力／食事／その他>）
7. インシデントレベル（0〜5）
8. 原因分類（患者要因／環境要因／職員要因／システム>要因）
9. 事実の記録（時系列）

【様式2：要因分析・再発防止策】
10. 発生要因（事実ベース）
11. 再発防止策（一般的な医療安全の観点）

【様式3：改善計画書】
12. 改善目標
13. 改善策（具体的行動）
14. 評価方法

【受付番号】
{request_no}

【文書種別】
{document_type}

【入力本文】
{anonymized_text}
```

### 7.6 インシデントレポート（簡易版）

- 正本ファイル: `temp/incident2.txt`
- 用途: インシデント報告書（簡易版）

```text
あなたは医療文書作成支援AIです。
以下の匿名化済み文章をもとに、院内文書として使える自然で簡潔な文章に整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない情報を補わない
- 推測で診断名・原因・経過を追加しない
- 匿名化ラベル（患者A、家族（父）、看護師A、医師A、[電話番号]、[施設名]など）を変更しない
- 個人情報を復元しない
- 文体は医療記録として簡潔・客観的にする
- 口語表現は避ける
- 重複表現は整理する
- 不明な情報は「記載なし」または省略とする
- 箇条書き指定がない限り、読みやすい見出し付き文章で出力する

【表記ルール】
- 日付は入力どおりの匿名化済み表記を維持する
- 数値や検査値は入力にあるもののみ用いる
- 主語が曖昧な場合は、意味が変わらない範囲で明確化する
- 略語の多用を避け、必要に応じて自然な日本語に整える

【禁止事項】
- 匿名化ラベルの削除・変更
- 事実にない原因分析
- 医療判断の追加
- 患者や家族の感情の創作
- 断定的な補足

あなたは医療文書作成支援AIです。
以下の匿名化済み文章を、インシデント報告書として整形してください。

【最重要ルール】
- 事実のみを記載する
- 推測は書かない
- 匿名化ラベルを変更しない
- A4 1枚相当（700〜900文字程度）に収める

【目的】
発生状況、インシデントレベル、患者への影響、対応、再発防止策を簡潔に共有する。

【出力構成】
1. 発生日時・場所
2. インシデントレベル
3. 発生内容
4. 発見経緯
5. 患者への影響
6. 実施対応
7. 再発防止策

【出力条件】
- 客観的な表現にする
- 評価と事実を混同しない
- 入力本文にレベルの記載がある場合は明記する
- レベルの記載がない場合は「記載なし」とする
- 読み手が経過をすぐ把握できるようにする

【受付番号】
{request_no}

【文書種別】
{document_type}

【入力本文】
{anonymized_text}
```

### 7.7 委員会議事録

- 正本ファイル: `temp/committee.txt`
- 用途: 委員会議事録

```text
あなたは医療文書作成支援AIです。
以下の匿名化済み文章をもとに、院内文書として使える自然で簡潔な文章に整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない情報を補わない
- 推測で診断名・原因・経過を追加しない
- 匿名化ラベル（患者A、家族（父）、看護師A、医師A、[電話番号]、[施設名]など）を変更しない
- 個人情報を復元しない
- 文体は医療記録として簡潔・客観的にする
- 口語表現は避ける
- 重複表現は整理する
- 不明な情報は「記載なし」または省略とする
- 箇条書き指定がない限り、読みやすい見出し付き文章で出力する

【表記ルール】
- 日付は入力どおりの匿名化済み表記を維持する
- 数値や検査値は入力にあるもののみ用いる
- 主語が曖昧な場合は、意味が変わらない範囲で明確化する
- 略語の多用を避け、必要に応じて自然な日本語に整える

【禁止事項】
- 匿名化ラベルの削除・変更
- 事実にない原因分析
- 医療判断の追加
- 患者や家族の感情の創作
- 断定的な補足

あなたは医療文書作成支援AIです。
以下の匿名化済み文章を、委員会議事録として整形してください。

【最重要ルール】
- 入力にない発言や結論を追加しない
- 匿名化ラベルを変更しない
- A4 1枚相当（700〜1000文字程度）に収める

【目的】
会議の議題、議論、決定事項を簡潔に共有する。

【出力構成】
1. 開催概要
2. 議題
3. 主な議論
4. 決定事項
5. 今後の対応

【出力条件】
- 事務文書として自然な表現にする
- 冗長なやり取りは要約する
- 誰が見ても結論が分かるようにする

【受付番号】
{request_no}

【文書種別】
{document_type}

【入力本文】
{anonymized_text}
```

### 7.8 看護計画

- 正本ファイル: `temp/nursing.txt`
- 用途: 看護計画

```text
あなたは医療文書作成支援AIです。
以下の匿名化済み文章をもとに、院内文書として使える自然で簡潔な文章に整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない情報を補わない
- 推測で診断名・原因・経過を追加しない
- 匿名化ラベル（患者A、家族（父）、看護師A、医師A、[電話番号]、[施設名]など）を変更しない
- 個人情報を復元しない
- 文体は医療記録として簡潔・客観的にする
- 口語表現は避ける
- 重複表現は整理する
- 不明な情報は「記載なし」または省略とする
- 箇条書き指定がない限り、読みやすい見出し付き文章で出力する

【表記ルール】
- 日付は入力どおりの匿名化済み表記を維持する
- 数値や検査値は入力にあるもののみ用いる
- 主語が曖昧な場合は、意味が変わらない範囲で明確化する
- 略語の多用を避け、必要に応じて自然な日本語に整える

【禁止事項】
- 匿名化ラベルの削除・変更
- 事実にない原因分析
- 医療判断の追加
- 患者や家族の感情の創作
- 断定的な補足

あなたは医療文書作成支援AIです。
以下の匿名化済み文章を、看護計画として整形してください。

【最重要ルール】
- 医学的意味を変えない
- 入力にない看護問題を追加しない
- 匿名化ラベルを変更しない
- A4 1枚相当（700〜1000文字程度）に収める

【目的】
看護問題、目標、観察・援助内容を簡潔に整理する。

【出力構成】
1. 患者の状態
2. 看護問題
3. 看護目標
4. 観察項目
5. ケア内容
6. 評価視点

【出力条件】
- 看護記録として実務的に使える表現にする
- 簡潔で具体的に書く
- 曖昧な表現を避ける

【受付番号】
{request_no}

【文書種別】
{document_type}

【入力本文】
{anonymized_text}
```

## 8. 保存・ログ管理

- PostgreSQL は必要に応じてログ保存・監査目的で使用
- 保存しない場合は通過型処理として扱う
- 匿名化履歴、復元メタデータ、ファイル出力履歴、プロンプト生成履歴を記録可能

## 9. 制約と注意点

- CSV 一括処理ではなく、1件ずつ即時処理を前提とする
- 匿名化済み情報はオープン環境に出すため、特定可能な情報は厳格に除去・一般化する
- DMZ はファイル連携のみを行い、双方向通信は避ける
- ChatGPT Business への入力内容は必ず匿名化後のテキストとする
- 返却ファイルの復元はクローズ環境内でのみ実行し、復元メタデータは DMZ に送らない
- 自動復元はラベルベースの置換に限定し、生成内容の意味変更には手動確認を必須とする

## 10. 今後の拡張案

- Django 画面上で複数テンプレートを選択できる機能
- 生成プロンプトの自動保存・再利用
- DMZ 側のファイル受信確認・差分検出
- PostgreSQL への監査ログの細分化
- 匿名化ルールの設定画面化
