# 現行システム仕様まとめ

## 1. システム概要

本システムは、クローズ環境で入力した医療・看護系文章を匿名化し、DMZ 経由で Open 環境へ渡して文章整形したうえで、再びクローズ環境へ戻して復元確認するためのプロトタイプである。

実装は次の3層で構成される。

- `close_side`: 匿名化、プロンプト作成、復元用メタデータ保存、DMZ 出力、返却結果の取り込み、テンプレート管理、匿名化ルール表示
- `open_side`: DMZ 上のプロンプト一覧表示、取り込み、ChatGPT 用表示、生成結果の返却DMZ出力
- `anonymizer/`: 匿名化・復元・プロンプト/結果 JSON 生成の共通ロジック

現在は本番 DMZ や PostgreSQL 連携ではなく、SQLite とローカルディレクトリを使った検証用構成である。

## 2. 実行環境

### 2.1 主要技術

- Python
- Django 4.2 系
- SQLite
- PyYAML

`requirements.txt` には `pandas`、`faker`、`paramiko` も含まれるが、現行の主フローでは使用していない。

### 2.2 Django 設定の要点

- `DEBUG = True`
- `LANGUAGE_CODE = 'ja'`
- `TIME_ZONE = 'Asia/Tokyo'`
- DB は SQLite
- ルート `/` は `/close/` にリダイレクト
- `NETWORK_POLICY_ENFORCED = True`
- `NETWORK_POLICY_ALLOW_LOOPBACK = DEBUG`
- `NETWORK_POLICY_TRUST_X_FORWARDED_FOR = True`

`NETWORK_POLICY` はアプリケーション層の補助制御であり、実運用ではネットワーク機器や OS 側のファイアウォールと併用する前提である。

### 2.3 ネットワーク区分

| 区分 | CIDR | 用途 |
|---|---|---|
| CloseSide | `192.168.50.0/24` | 機密情報を扱う内部領域 |
| OpenSide | `192.168.110.0/24` | 生成 AI 利用側 |
| DMZ | `192.168.150.0/24` | ファイル中継領域 |

## 3. 画面と URL

主な URL は以下のとおりである。

| URL | 機能 |
|---|---|
| `/close/` | CloseSide メニュー |
| `/close/anonymize/` | 匿名化入力・結果確認 |
| `/close/dmz/export/` | プロンプト JSON を Close→Open DMZ へ出力 |
| `/close/dmz/results/` | 返却 DMZ ファイル一覧 |
| `/close/dmz/results/import/` | 返却 JSON の取り込み |
| `/close/dmz/results/history/` | 取り込み済み結果履歴 |
| `/close/prompts/` | 手動プロンプト一覧 |
| `/close/prompts/new/` | 手動プロンプト作成 |
| `/close/templates/` | テンプレート一覧 |
| `/close/templates/new/` | テンプレート作成 |
| `/close/templates/<pk>/edit/` | テンプレート編集 |
| `/close/anonymization-rules/` | 匿名化ルール表示 |
| `/close/logs/` | 操作ログ一覧（管理者のみ） |
| `/open/` | OpenSide メニュー |
| `/open/dmz/list/` | DMZ 上のプロンプト一覧 |
| `/open/dmz/import/` | ファイル名指定で取り込み |
| `/open/imported/<filename>/` | 取り込み済みプロンプト表示 |
| `/open/imported/<filename>/result/` | 生成結果を返却 DMZ へ出力 |
| `/open/logs/` | 操作ログ一覧（管理者のみ） |

## 4. データモデル

### 4.1 RestoreMetadata

匿名化 1 セッションの復元メタデータとプロンプト JSON を保持する。

| フィールド | 内容 |
|---|---|
| `source_id` | 匿名化処理単位の ID（ユニーク） |
| `template_type` | テンプレート種別 |
| `restore_map` | 匿名化後文言 → 元文言 の対応表 |
| `prompt_json` | DMZ 出力用のプロンプト JSON |
| `owner` | 所有者 |
| `status` | `draft` / `sent_to_dmz` / `imported_to_open` / `returned_to_dmz` / `imported_to_close` |
| `created_at` / `updated_at` | 作成・更新日時 |

### 4.2 RestoredResult

OpenSide から戻った結果 JSON と、復元後テキストを保持する。

| フィールド | 内容 |
|---|---|
| `source_id` | 対応する匿名化セッション ID |
| `result_id` | 返却 JSON の ID |
| `template_type` | 文書種別 |
| `result_text` | ChatGPT 生成結果本文 |
| `restored_text` | `restore_map` 適用後の本文 |
| `result_json` | 返却 JSON 原文 |
| `imported_filename` | 取り込み元ファイル名 |
| `reviewer` | レビュワー |
| `owner` | 所有者 |
| `status` | `imported` / `deleted` |

### 4.3 Prompt

手動プロンプトを含む再利用用プロンプト保存モデルである。

| フィールド | 内容 |
|---|---|
| `name` | プロンプト名 |
| `content` | プロンプト本文 |
| `source_id` | 匿名化セッション ID または手動生成 ID |
| `owner` | 所有者 |
| `status` | `draft` / `sent_to_dmz` / `imported_to_open` |

### 4.4 Template

テンプレート正本 `webapp/anonymizer_app/prompt_templates/*.txt` を DB に同期したキャッシュである。

| フィールド | 内容 |
|---|---|
| `template_type` | テンプレート種別 |
| `name` | 表示名 |
| `description` | 説明 |
| `source_filename` | 正本 txt ファイル名 |
| `content` | 結合済みテンプレート本文 |
| `basic_content` | 共通ベース部分 |
| `additional_content` | 文書種別ごとの追加部分 |
| `created_by` | 作成者 |

現在のテンプレート種別は以下である。

- 入院時サマリー
- 退院時サマリー
- 中間サマリー￥ｋ
- インシデントレポート
- 委員会議事録
- 看護計画

### 4.5 AnonymizationRule / OperationLog

- `AnonymizationRule`: 画面表示用の匿名化ルール本文
- `OperationLog`: 操作種別、対象 ID、送信元 IP、取込元 IP、結果、エラー内容、詳細 JSON を記録する監査ログ

## 5. 匿名化エンジン

匿名化処理は `anonymizer/modules/anonymize.py` が担う。主な処理は次のとおり。

1. 全角英数字と一部記号を半角へ正規化する
2. 日付を月単位の幅表記へ一般化する
3. 時刻を `午前（時刻1）` / `午後（時刻1）` のようなラベルへ置換する
4. 住所を `○○市内` / `○○区内` / `住所` に一般化する
5. 人名を `患者A`、`患者B` のようなラベルへ置換する
6. `anonymizer/config/columns.yml` の `entity_replacements` を使って、薬剤名などをカテゴリラベルへ置換する

`restore_map` は原則として `匿名化後文言 → 元文言` の向きで保持する。全角英数字の半角化は復元対象に戻さない方針である。

`anonymize_text()` の戻り値は次の 3 要素である。

- `text`: 匿名化後本文
- `restore_map`: 復元用対応表
- `metadata`: `template_type`、`anonymized_at`、`hash_id`

### 5.1 置換対象の例

`anonymizer/config/columns.yml` では、次のようなパターンを扱う。

- 氏名パターン
- 住所パターン
- 日付パターン
- 時刻パターン
- 薬剤名などの個別語

## 6. プロンプトとテンプレート

### 6.1 プロンプト生成

`build_prompt_payload()` は次の構造を返す。

```json
{
  "id": "source_id",
  "template_type": "入院時サマリー",
  "title": "入院時サマリー",
  "prompt": "...",
  "prompt_text": "...",
  "content": { "text": "..." },
  "metadata": {
    "source_id": "source_id",
    "created_at": null
  }
}
```

CloseSide で実際に出力するときは、`metadata` に `owner_user_id`、`owner_username`、`template_name`、`sent_by`、`sent_at` などが追加される。

### 6.2 テンプレートファイル

`webapp/anonymizer_app/prompt_templates/default.txt` を共通ベースとし、文書種別ごとの txt を結合して使う。

現在の主なファイルは以下である。

- `default.txt`
- `admission.txt`
- `discharge.txt`
- `midterm.txt`
- `incident.txt`
- `incident2.txt`
- `committee.txt`
- `nursing.txt`

テンプレート DB は `python manage.py load_prompt_templates` で同期できる。フォーム表示時にも同期処理が走る。

### 6.3 匿名化ルール表示

`webapp/anonymizer_app/prompt_templates/anonymization_rules.md` を元に、`AnonymizationRule` が未登録ならその内容を画面表示する。管理画面から本文を差し替えることもできる。

## 7. DMZ とファイル連携

### 7.1 ディレクトリ

- `dmz/close_to_open/`: CloseSide から OpenSide へ渡すプロンプト置き場
- `dmz/open_to_close/`: OpenSide から CloseSide へ返す結果置き場
- `webapp/anonymizer_app/logs/`: OpenSide 取り込み後のローカルコピー

### 7.2 ファイル名

- プロンプト: `prompt_{source_id}.json`
- 返却結果: `result_{source_id}_{YYYYMMDD_HHMMSS}.json`

### 7.3 送受信の流れ

1. CloseSide が `RestoreMetadata.prompt_json` を `dmz/close_to_open/` に書き出す
2. OpenSide が DMZ から取り込み、`logs/` にコピーして元ファイルを削除する
3. OpenSide が ChatGPT 結果を入力し、返却 JSON を `dmz/open_to_close/` に出力する
4. CloseSide が返却 JSON を取り込み、`restore_map` を使って復元する
5. 取り込み後は DMZ 上の返却 JSON を削除する

### 7.4 手動プロンプト

`Prompt` 画面から作成した手動プロンプトも DMZ 送信できる。`source_id` が無い場合は `prompt_manual_{pk}_{8hex}` が発番され、空の `restore_map` で保存される。

## 8. 権限と監査

- CloseSide / OpenSide はログイン必須
- `user.is_staff` または `user.is_superuser` のみがユーザー一覧と操作ログを参照できる
- データ参照は所有者ベースで制御し、管理者は全件参照できる
- `NetworkSegmentPolicyMiddleware` が IP アドレスを確認し、対象パスごとにアクセスを制限する
- `OperationLog` が匿名化作成、DMZ 送信、取り込み、削除、ネットワーク拒否などを記録する

## 9. CLI

`anonymizer/cli.py` には次のサブコマンドがある。

- `anonymize`: プロンプト JSON を生成する
- `convert`: ChatGPT 出力テキストを result JSON に変換する
- `restore`: result JSON と復元メタデータから復元テキストを生成する

注意点として、現行の `anonymize` コマンドは `anonymize_text()` を呼ばず、渡された本文をそのまま `build_prompt_payload()` に渡している。コマンド名と実処理が一致していない。

## 10. 未完了課題

現時点で確認できる主な未完了課題は以下である。

- 本番 DMZ 連携、共有ストレージ連携、ウイルスチェックが未実装
- PostgreSQL 化は未実施で、現行は SQLite
- CLI `anonymize` は実匿名化を行っていない
- 匿名化ロジックは正規表現ベースで、網羅性と誤検知/取りこぼしの検証が必要
- テンプレートは txt を直接更新する運用で、版管理や承認フローは未整備
- 自動テストコードがこのワークツリー内では確認できないため、匿名化・復元・DMZ 連携の回帰検証は手動依存である
- `requirements.txt` の一部依存は現行フローで未使用のままである

## 11. まとめ

現行システムは、CloseSide で匿名化と復元用メタデータを作成し、OpenSide で ChatGPT に渡す文面を扱い、返却結果を CloseSide で復元確認するローカル検証構成である。

仕様書上は将来の本番連携を想定していても、現状の実装は SQLite とローカル DMZ ディレクトリを前提にしている点を基準に読む必要がある。
