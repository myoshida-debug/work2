# 現行システム仕様まとめ

## 1. システム概要

本システムは、医療・看護系文書をクローズ環境で匿名化し、DMZ 経由で Open 環境へ渡すためのプロトタイプ実装である。

現在の実装は、Django Web アプリケーションと Python CLI から構成される。Django 側は CloseSide と OpenSide の2アプリに分離されている。

- CloseSide: 文章入力、匿名化、復元用メタデータ保存、プロンプト JSON 生成、DMZ への出力を担当する
- OpenSide: DMZ に配置された匿名化済み JSON の一覧表示と取り込みを担当する

CLI 側では、プロンプト JSON 生成、ChatGPT 出力テキストの JSON 化、復元処理を扱う。

現状は本番の DMZ・PostgreSQL 連携ではなく、SQLite とローカルディレクトリを使った検証用構成である。

## 2. ディレクトリ構成

```text
work2/
├── README.md
├── requirements.txt
├── 匿名化フロー仕様書.md
├── anonymizer/
│   ├── cli.py
│   ├── config/
│   │   └── columns.yml
│   └── modules/
│       ├── anonymize.py
│       ├── checker.py
│       ├── loader.py
│       ├── reporter.py
│       └── utils.py
├── webapp/
│   ├── manage.py
│   ├── db.sqlite3
│   ├── anonflow/
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   ├── close_side/
│   │   ├── apps.py
│   │   ├── urls.py
│   │   └── views.py
│   ├── open_side/
│   │   ├── apps.py
│   │   ├── urls.py
│   │   └── views.py
│   └── anonymizer_app/
│       ├── models.py
│       ├── forms.py
│       ├── modules/
│       │   └── anonymize.py
│       ├── prompt_templates/
│       └── templates/
└── dmz/
    └── close_to_open/
```

## 3. 実行環境

### 3.1 主要技術

- Python
- Django
- SQLite
- PyYAML
- paramiko

### 3.2 Django 設定

Django プロジェクトは `webapp/anonflow` に配置されている。

主な設定は以下のとおり。

- `DEBUG = True`
- `LANGUAGE_CODE = 'ja'`
- `TIME_ZONE = 'Asia/Tokyo'`
- DB は SQLite
- アプリケーションは `close_side`, `open_side`, `anonymizer_app`
- `anonymizer_app` は DB モデル、フォーム、既存テンプレート、匿名化ラッパーを持つ共通層として扱う

設定ファイル:

- `webapp/anonflow/settings.py`

## 4. Web アプリ仕様

### 4.1 URL 構成

Web アプリの主要 URL は、CloseSide が `webapp/close_side/urls.py`、OpenSide が `webapp/open_side/urls.py` に定義されている。

ルート `/` は CloseSide のトップ画面 `/close/` へリダイレクトする。

| URL | 機能 |
|---|---|
| `/` | `/close/` へリダイレクト |
| `/close/` | CloseSide 匿名化入力画面 |
| `/close/dmz/export/` | CloseSide から DMZ へのファイル出力 |
| `/close/dmz/results/` | OpenSide から返却された result JSON の一覧・取り込み |
| `/close/dmz/results/import/` | result JSON 取り込み・復元 |
| `/close/download/prompt/<source_id>/` | プロンプト JSON ダウンロード |
| `/close/download/restore/<source_id>/` | 復元メタデータ JSON ダウンロード |
| `/close/prompts/` | プロンプト一覧 |
| `/close/prompts/new/` | プロンプト作成 |
| `/close/prompts/<pk>/edit/` | プロンプト編集 |
| `/close/templates/` | テンプレート一覧 |
| `/close/templates/new/` | テンプレート作成 |
| `/close/templates/<pk>/edit/` | テンプレート編集 |
| `/close/templates/<template_name>/` | テンプレート詳細 |
| `/close/anonymization-rules/` | 匿名化ルール表示 |
| `/close/api/templates/<template_name>/` | テンプレートプレビュー API |
| `/open/` | OpenSide DMZ ファイル一覧 |
| `/open/dmz/list/` | OpenSide DMZ ファイル一覧 |
| `/open/dmz/import/` | OpenSide DMZ ファイル取り込み |
| `/open/imported/<filename>/` | 取り込み済みプロンプト表示・ChatGPT貼り付け |
| `/open/imported/<filename>/result/` | ChatGPT生成結果を result JSON 化して返却DMZへ出力 |

### 4.2 匿名化入力画面

トップ画面では、文書テンプレートと入力テキストを受け取る。

処理の流れは以下のとおり。

1. `AnonymizeForm` でテンプレートと本文を受け取る
2. `anonymize_text()` で本文を匿名化する
3. `source_id` を生成する
4. プロンプト JSON を生成する
5. 復元用メタデータを作成する
6. `RestoreMetadata` に保存する
7. 結果画面に、元文、匿名化後文、差分、JSON を表示する

該当実装:

- `webapp/close_side/views.py`
- `webapp/anonymizer_app/modules/anonymize.py`
cd 
### 4.3 OpenSide 画面

OpenSide は、CloseSide から `dmz/close_to_open/` に出力された JSON ファイルを扱う。

主な処理は以下である。

1. DMZ ディレクトリのファイル一覧を表示する
2. 指定されたファイルを OpenSide 側の `logs/` に取り込む
3. `prompt` / `prompt_text` を画面に表示し、ChatGPT へコピーできるようにする
4. ChatGPT の生成結果を貼り付けて、`result_text` を含む result JSON を作成する
5. result JSON を `dmz/open_to_close/` に出力する

該当実装:

- `webapp/open_side/views.py`

### 4.4 CloseSide 返却取り込み画面

CloseSide は、OpenSide から `dmz/open_to_close/` に返却された result JSON を取り込む。

主な処理は以下である。

1. 返却DMZディレクトリのファイル一覧を表示する
2. 指定された result JSON を読み込む
3. `source_id` で `RestoreMetadata` を検索する
4. `result_text` に `restore_map` を適用して復元済みテキストを生成する
5. `RestoredResult` に取り込み結果と復元済みテキストを保存する
6. ChatGPT生成結果と復元済みテキストを画面に並べて表示する

## 5. データモデル

データモデルは `webapp/anonymizer_app/models.py` に定義されている。

### 5.1 RestoreMetadata

匿名化処理ごとの復元メタデータとプロンプト JSON を保存する。

| フィールド | 内容 |
|---|---|
| `source_id` | 匿名化処理単位の ID |
| `template_type` | 使用テンプレート種別 |
| `restore_map` | 匿名化・復元用対応表 |
| `prompt_json` | 生成されたプロンプト JSON |
| `owner` | 作成者 |
| `status` | 作成済み、DMZ送信済み、取込済みなどの状態 |
| `created_at` | 作成日時 |
| `updated_at` | 更新日時 |

### 5.2 RestoredResult

ChatGPT生成結果の返却取り込みと復元済みテキストを保存する。

| フィールド | 内容 |
|---|---|
| `source_id` | 対応する匿名化処理単位の ID |
| `result_id` | result JSON の ID |
| `template_type` | 使用テンプレート種別 |
| `result_text` | ChatGPT の生成結果本文 |
| `restored_text` | 復元マップ適用後の本文 |
| `result_json` | 取り込んだ result JSON |
| `imported_filename` | 取り込み元ファイル名 |
| `reviewer` | レビュワー |
| `owner` | 対象データの所有者 |
| `status` | 取込済み、削除済みなどの状態 |
| `created_at` | 作成日時 |
| `updated_at` | 更新日時 |

### 5.3 Prompt

再利用可能なプロンプト本文を保存する。

| フィールド | 内容 |
|---|---|
| `name` | プロンプト名 |
| `content` | プロンプト本文 |
| `source_id` | 匿名化処理単位の ID |
| `owner` | 作成者 |
| `status` | 作成済み、DMZ送信済み、OpenSide取込済みなどの状態 |
| `created_at` | 作成日時 |
| `updated_at` | 更新日時 |

### 5.4 Template

文書テンプレートのDBキャッシュを保存する。テンプレートの正本は `webapp/anonymizer_app/prompt_templates/*.txt` であり、DB は画面表示・検索のために同期される派生データとして扱う。

| フィールド | 内容 |
|---|---|
| `template_type` | テンプレート種別 |
| `name` | テンプレート名 |
| `description` | 説明 |
| `source_filename` | 正本txtファイル名 |
| `content` | 結合済みテンプレート本文 |
| `basic_content` | 基本テンプレート |
| `additional_content` | 追加部分 |
| `created_by` | 作成者 |
| `created_at` | 作成日時 |
| `updated_at` | 更新日時 |

対応するテンプレート種別は以下である。

- 入院時サマリー
- 退院時サマリー
- 中間サマリー
- インシデントレポート
- 委員会議事録
- 看護計画

プロンプト用テンプレートファイルは `webapp/anonymizer_app/prompt_templates/` に配置される。

| ファイル | 用途 |
|---|---|
| `default.txt` | 共通ルールと汎用院内文書テンプレート |
| `admission.txt` | 入院時サマリー |
| `discharge.txt` | 退院時サマリー |
| `midterm.txt` | 中間サマリー |
| `incident.txt` | インシデントレポート（様式1-3） |
| `incident2.txt` | インシデントレポート（簡易版） |
| `committee.txt` | 委員会議事録 |
| `nursing.txt` | 看護計画 |

DB キャッシュへの登録・更新は以下の管理コマンドで行う。

```bash
cd webapp
python manage.py load_prompt_templates
```

このコマンドは `default.txt` の共通ルール部分を `basic_content`、各文書別テンプレートの追加部分を `additional_content` として保存する。

アプリ画面でテンプレートを作成・編集する場合も、保存先はtxt正本である。保存後にDBキャッシュを同期する。不要になったDBキャッシュ行を削除したい場合は、以下を実行する。

```bash
cd webapp
python manage.py load_prompt_templates --prune-stale
```

### 5.5 AnonymizationRule

匿名化ルール本文を保存する。

| フィールド | 内容 |
|---|---|
| `name` | ルール名 |
| `content` | ルール本文 |
| `updated_at` | 更新日時 |

### 5.6 OperationLog

作成、送信、取込、削除、復元などの操作履歴を保存する。

| フィールド | 内容 |
|---|---|
| `actor` | 操作者 |
| `actor_username` | 操作者ID |
| `action` | 操作種別 |
| `target_type` | 対象種別 |
| `target_id` | プロンプトID、生成文章IDなど |
| `source_ip` | 操作元IPアドレス |
| `import_source_ip` | 取込操作元IPアドレス |
| `result` | 成功、失敗 |
| `error_message` | 失敗時の詳細 |
| `details` | 補足情報 |
| `created_at` | 実行日時 |

## 6. 匿名化処理仕様

### 6.1 共通匿名化処理

匿名化処理は、外側の共通パッケージ `anonymizer/modules/anonymize.py` に集約されている。

`webapp/anonymizer_app/modules/anonymize.py` は互換性維持用の薄いラッパーであり、既存の Django 側 import を壊さずに、実処理を共通パッケージへ委譲する。

主な匿名化対象は以下のとおり。

| 対象 | 処理内容 |
|---|---|
| 全角英数字・記号 | 半角へ正規化 |
| 日付 | 上旬・中旬・下旬へ一般化 |
| 時刻 | 午前・午後へ一般化 |
| 住所 | 市区町村レベルへ一般化 |
| 氏名 | `患者A氏` などのラベルに置換 |
| 薬剤名など | 設定ファイルに基づきカテゴリラベルへ置換 |

処理は正規表現ベースであり、軽量なヒューリスティックとして実装されている。

薬剤名などの置換対象は `anonymizer/config/columns.yml` に定義されている。

例:

| 用語 | カテゴリ |
|---|---|
| リスペリドン | 抗精神病薬 |
| オランザピン | 抗精神病薬 |
| ジアゼパム | 抗不安薬 |

## 7. プロンプト JSON 仕様

### 7.1 プロンプト JSON

Web 側と CLI 側は、共通パッケージの `build_prompt_payload()` を使ってプロンプト JSON を生成する。`prompt` / `prompt_text` には、`webapp/anonymizer_app/prompt_templates/*.txt` のtxt正本テンプレートを反映し、`{request_no}`、`{document_type}`、`{anonymized_text}` などのプレースホルダを生成時に置換する。

生成される JSON は以下の形式である。

```json
{
  "id": "prompt_xxx",
  "template_type": "退院時サマリー",
  "title": null,
  "prompt": "ChatGPT に貼り付けるプロンプト本文",
  "prompt_text": "ChatGPT に貼り付けるプロンプト本文",
  "content": {
    "text": "匿名化済み本文"
  },
  "metadata": {
    "source_id": "prompt_xxx",
    "created_at": null
  }
}
```

### 7.2 result JSON

OpenSide の返却画面、および CLI 側の `convert` コマンドでは、ChatGPT 出力テキストを以下の形式に変換する。

```json
{
  "id": "result_prompt_xxx",
  "source_id": "prompt_xxx",
  "result_text": "ChatGPT の出力本文",
  "metadata": {
    "processed_at": null,
    "reviewer": "user_a"
  }
}
```

## 8. DMZ 連携仕様

### 8.0 ネットワーク設計・セキュリティ方針

#### 基本方針

- CloseSide、DMZ、OpenSide は別セグメントに分離する
- CloseSide と OpenSide は直接通信しない
- データ連携は必ず DMZ を経由する
- DMZ は能動的に送信せず、データの一時保管領域として扱う
- CloseSide / OpenSide が DMZ へアクセスし、必要なデータを取り込む
- CloseSide / OpenSide 側から DMZ をポーリングまたは API 呼び出しで取り込む
- ファイアウォールで通信元、通信先、ポートを制限する

#### セグメント設計

| 区分 | ネットワーク帯 | 用途 |
|---|---:|---|
| CloseSide | `192.168.50.0/24` | 機密情報を扱う内部領域 |
| DMZ | `192.168.150.0/24` | CloseSide / OpenSide 間の中継領域 |
| OpenSide | `192.168.110.0/24` | 生成AI処理を行う外部連携領域 |

#### 通信制御

| 通信元 | 通信先 | 用途 | 方針 |
|---|---|---|---|
| CloseSide | DMZ | プロンプト送信、生成文章取込 | 許可 |
| OpenSide | DMZ | プロンプト取込、生成文章送信 | 許可 |
| DMZ | CloseSide | 能動送信 | 原則禁止。CloseSide からの取込方式を推奨 |
| DMZ | OpenSide | 能動送信 | 原則禁止。OpenSide からの取込方式を推奨 |
| CloseSide | OpenSide | 直接通信 | 禁止 |
| OpenSide | CloseSide | 直接通信 | 禁止 |

#### 設計上の扱い

DMZ は以下のような「置き場」として設計する。

```text
CloseSide
  ↓ プロンプト送信
DMZ
  ↑ OpenSideが取り込み

OpenSide
  ↓ AI生成文章送信
DMZ
  ↑ CloseSideが取り込み
```

#### ログ記録項目

操作ログには以下を記録する。

| 項目 | 内容 |
|---|---|
| 操作者ID | 作成、送信、取込、削除したユーザー |
| 操作種別 | 送信、取込、削除、復元など |
| 対象ID | プロンプトID、生成文章ID |
| 送信元IP | 操作元IPアドレス |
| 取込元IP | 取込操作元IPアドレス |
| 実行日時 | 操作日時 |
| 結果 | 成功、失敗 |
| エラー内容 | 失敗時の詳細 |

現行 Web アプリでは、`NETWORK_POLICY` 設定と `NetworkSegmentPolicyMiddleware` により、`/close/` は CloseSide セグメント、`/open/` は OpenSide セグメントからのアクセスに制限する。開発環境では `NETWORK_POLICY_ALLOW_LOOPBACK = True` により localhost からの確認を許可する。

### 8.1 現行実装

DMZ 連携は、実ネットワーク転送ではなくローカルディレクトリを使う。

使用ディレクトリ:

```text
dmz/close_to_open/
dmz/open_to_close/
```

### 8.2 エクスポート

CloseSide の `/close/dmz/export/` では、保存済みの `RestoreMetadata.prompt_json` をローカル DMZ ディレクトリへ出力する。

出力ファイル名:

```text
prompt_{source_id}.json
```

### 8.3 インポート

OpenSide の `/open/dmz/import/` では、指定されたファイル名を `dmz/close_to_open/` から読み取り、`webapp/open_side/logs/` にコピーする。

### 8.4 一覧表示

OpenSide の `/open/dmz/list/` では、`dmz/close_to_open/` 配下のファイル名、サイズ、更新日時を表示する。

### 8.5 result JSON 返却

OpenSide の `/open/imported/<filename>/result/` では、ChatGPT 生成結果を result JSON に変換し、`dmz/open_to_close/` に出力する。

出力ファイル名:

```text
result_{source_id}_{YYYYMMDD_HHMMSS}.json
```

CloseSide の `/close/dmz/results/` では、`dmz/open_to_close/` 配下の result JSON を取り込み、保存済みの `RestoreMetadata.restore_map` を使って復元済みテキストを生成する。

## 9. CLI 仕様

CLI は `anonymizer/cli.py` に実装されている。

### 9.1 anonymize

プロンプト JSON を生成する。

```bash
python -m anonymizer.cli anonymize --template "入院時サマリー" --text "本文" --output prompt.json
```

注意: 現状の CLI `anonymize` は、実際の匿名化処理を呼び出していない。渡された `text` をそのままプロンプト JSON に格納する。

### 9.2 convert

ChatGPT の出力テキストを result JSON に変換する。

```bash
python -m anonymizer.cli convert --input output.txt --source-id prompt_xxx --reviewer user_a --output result.json
```

### 9.3 restore

result JSON と復元メタデータを使って復元テキストを生成する。

```bash
python -m anonymizer.cli restore --result result.json --restore-metadata restore_meta.json --output restored.txt
```

## 10. 現行仕様と設計書との差分

| 項目 | 仕様書上の想定 | 現行実装 |
|---|---|---|
| DB | PostgreSQL | SQLite |
| DMZ | 実 DMZ・共有フォルダ・ウイルスチェック | ローカル `dmz/close_to_open/` |
| JSON 形式 | `id`, `prompt`, `prompt_text`, `content`, `metadata` | 共通 `build_prompt_payload()` で生成 |
| 復元処理 | クローズ環境内で実行 | CLI に復元処理あり、Web 画面には本格復元機能なし |
| 匿名化ロジック | 統一モジュール想定 | 外側の `anonymizer/` に集約済み |
| SFTP | DMZ 転送候補 | `paramiko` import のみで未使用 |
| ウイルスチェック | DMZ で実施 | 未実装 |

## 11. 注意点

### 11.1 既存データの restore_map

現行コードでは、Web 側も共通匿名化処理を使うため、`restore_map` は「匿名化後文言 → 元文言」の向きで生成される。

ただし、共通化前に SQLite に保存済みの `RestoreMetadata` がある場合、その `restore_map` は旧 Web 実装の形式で保存されている可能性がある。

既存データを復元処理に使う場合は、生成日時または JSON 内容を確認する必要がある。

### 11.2 Web 側の匿名化モジュール

`webapp/anonymizer_app/modules/anonymize.py` は、実装本体ではなく互換ラッパーである。

既存の import パスを維持するために残しており、実際の処理は `anonymizer/modules/anonymize.py` が担う。

### 11.3 CLI anonymize は匿名化していない

CLI の `anonymize` コマンドは `build_prompt_payload()` を呼ぶだけで、`anonymize_text()` を呼び出していない。

コマンド名と実際の処理に差があるため、運用時には注意が必要である。

## 12. まとめ

現在のシステムは、仕様書に記載された匿名化フローのうち、クローズ環境側の基本機能をローカル環境で検証するための実装である。

実装済みの中心機能は以下である。

- Django 画面からの単文入力
- 正規表現ベースの匿名化
- 匿名化差分表示
- 復元用メタデータ保存
- プロンプト JSON 生成
- ローカル DMZ ディレクトリへの出力
- テンプレート管理
- 匿名化ルール表示
- CLI による JSON 生成・変換・復元

一方で、以下は未整備または改善対象である。

- PostgreSQL 化
- DMZ の実転送
- ウイルスチェック
- Web 側の返却 JSON 取り込みと復元画面
- CLI `anonymize` の実匿名化対応
